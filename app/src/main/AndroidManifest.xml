<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dualscreen">

    <!-- 声明支持多屏显示 -->
    <uses-feature
        android:name="android.software.activities_on_secondary_displays"
        android:required="true" />

    <!-- 必要权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".CarApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DualScreenCar"
        tools:targetApi="33">

        <!-- 中控屏主Activity -->
        <activity
            android:name=".ui.CenterConsoleActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CenterConsole">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 仪表屏Activity -->
        <activity
            android:name=".ui.InstrumentClusterActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.InstrumentCluster" />

        <!-- 导航Activity -->
        <activity
            android:name=".ui.NavigationActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CenterConsole" />

        <!-- 媒体播放Activity -->
        <activity
            android:name=".ui.MediaActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CenterConsole" />

        <!-- 车辆设置Activity -->
        <activity
            android:name=".ui.VehicleSettingsActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout"
            android:exported="false"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.CenterConsole" />

        <!-- 开机自启动服务 -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 车辆数据模拟服务 -->
        <service
            android:name=".service.VehicleDataService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
